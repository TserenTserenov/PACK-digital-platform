---
id: DP.MAP.003
name: Guide Pipeline Source Map
name_ru: "Карта источников конвейера генерации руководств и методических материалов"
name_en: "Guide Pipeline Source Map"
type: map
status: draft
maturity: draft-pending-F1
summary: "Карта источников конвейера генерации руководств и методических материалов (РП521): один механизм отбора, две конфигурации; три подпотока данных; происхождение рубрик; аудит машиночитаемости программ"
created: 2026-08-11
updated: 2026-08-11
source: "РП521 Ф0, пир-сессия 2026-08-11-24-kimi-wp521-source-map (консенсус Claude+Kimi)"
related:
  uses: [DP.MAP.002]
  see_also: ["DATA-RESIDENCY.md §3 (типы 2.1-2.4)", "DS-my-strategy/inbox/WP-521/WP-521.md", "DS-autonomous-agents/inbox/selection-config-contract-draft.md"]
---

# [DP.MAP.003] Карта источников конвейера генерации руководств и методических материалов

> **Назначение:** проверяемое обещание конвейера РП521 — откуда каждый из двух выходов
> (персональное руководство / методический материал) берёт данные, откуда берутся критерии
> качества (рубрики) и какие программы-источники допустимы как основной материал.
> Каждая строка фальсифицируема: можно открыть источник и убедиться.
>
> **Статус зрелости:** `draft-pending-F1` — строки с пометкой `[spike-validated: no]` являются
> проектными гипотезами до спайк-прогона одного занятия (нулевой шаг Ф1 РП521). После спайка
> пометки снимаются одним коммитом — это и есть условие снятия статуса.

## §А. Развязка: механизм один, конфигурации две

Механизм отбора материала — ОДИН код-путь из 5 шагов. Различия двух выходов — только
декларативные конфигурации; переопределяемы шаги 1/3/5, шаг 4 закрыт замком
(`mechanism.locked_steps: [4]`, валидатор отклоняет оверлей с ключами `steps.4.*`).

| Шаг механизма | Руководство (по запросу) | Методический материал (ежедневная петля) |
|---|---|---|
| 1. Цель сборки | квалификация / ступень (человек сам назвал программу) | текущее узкое место (bottleneck_slot) |
| 2. Основной материал + проверка предпосылок | общий: машинная проверка обязательна (см. §Г) | общий: машинная проверка обязательна (см. §Г) |
| 3. Внешняя поддержка из других программ | выключена | включена, ограниченно, с проверкой готовности |
| 4. Происхождение фрагмента + content_hash | **общий, не переопределяем** | **общий, не переопределяем** |
| 5. Скрытие меток программ в интерфейсе | выключено | включено (источник показывается по запросу) |

Схема происхождения фрагмента — 10 полей (решение пилота 11.08, здесь НЕ расширяется):
`fragment_id, role (main|support), source_program, source_unit, source_version, content_hash,
position_in_sequence, selection_basis (узкое место | квалификация), prerequisites_checked[],
rubric_ref`; плюс `assembled_by_run` на уровне объекта занятия/сборки.

## §Б. Карта данных: три подпотока

Конвейер собирает документ из трёх подпотоков; смешение происходит только в собранном
документе, поэтому шаг 4 (происхождение на каждый фрагмент) обязателен обоим выходам.
Ось типов 2.1-2.4 (`DATA-RESIDENCY.md §3`) типизирует только подпоток 1; учебный материал
программ — платформенное доменное знание, вне этой оси; мета-конфигурация — вне оси,
фиксируется через `assembled_by_run` (id прогона держит версию конфигурации), фрагментов
не порождает.

### Б.1 Личный контекст (определяет ЦЕЛЬ сборки и адаптацию)

| Что берём | Тип | Откуда физически | Формат | Частота | Писатель данных | Выход |
|---|---|---|---|---|---|---|
| Профиль RCS: rcs_current, индикаторы, mastery_by_area (`calculated_profile`) | 2.1 | Neon (indicators) | SQL | live при сборке | воркеры индикаторов платформы | оба |
| Производные двойника (`digital_twins → 3_derived`, rcs_history) | 2.1/2.2 | Neon (indicators) | SQL | live | конвейеры цифрового двойника | оба |
| События активности 7 дней (`learning.domain_event`) | 2.2 | Neon (learning) | SQL | live | поверхности/боты платформы | методичка `[spike-validated: no]` |
| Переходы ступеней (`learning.stage_transitions`) | 2.1 | Neon (learning) | SQL | live | stage_evaluator | оба |
| Диагностика: cp_profile, bottleneck_slot (`learning.cp_assessments`) | 2.1 | Neon (learning) | SQL | live | диагностика (бот/VS Code канон; браузер через cutover) | методичка (шаг 1) |
| Личные источники знаний (`knowledge.user_sources`: github owner/repo) | 2.3 | Neon (knowledge) | SQL | live | привязка пользователя | оба `[spike-validated: no]` |
| Идентификация (`ory_identity` → telegram_id) | 2.1 (PII) | Neon | SQL | live | Ory / привязка | оба; **только маршрутизация доставки — в собранный документ не попадает** |
| Вчерашняя панель `panel/{date}.md` (carry-over незакрытого) | 2.4 | GitHub-репо пилота personal-guide | GitHub API / file | суточный срез | Портной (вчерашний рендер) + пилот | методичка |
| Заметка выполнения `assignment-log.md` | 2.4 | GitHub-репо пилота personal-guide | file | по событию | пилот | методичка |
| Конфиг пилотов `config/pilot-guides.yaml` (опт-аут, оверрайды) | 2.1 | DS-autonomous-agents (файл) | file | по коммиту | пилот/оператор | оба |

### Б.2 Доменный учебный материал (определяет СОДЕРЖАНИЕ; вне оси 2.x)

| Что берём | Откуда физически | Формат | Частота | Писатель | Выход |
|---|---|---|---|---|---|
| Карточки CAT.001 (91 карточка-мем на 11.08.2026) / CAT.002 (10 практик) / CAT.003 | `DS-principles-curriculum/data/curriculum/CAT.00{1,2,3}/` | file (md + frontmatter) | по коммиту каталога | авторы каталога | оба |
| Задания МШС-программ `programs/{lr,rr,ir}/assignments.yaml` | DS-autonomous-agents (`agents/tailor/programs/`) | file (yaml) | по коммиту | авторы программ | методичка |
| Разобранный пользовательский источник `topic_sections.yaml` (decomposer, Mode Б) | guide-kit | file | не подключён | — | **резерв** (см. §Г) |

### Б.3 Мета-конфигурация сборки (управляет, фрагментов не порождает)

| Что | Откуда | Фиксация в происхождении |
|---|---|---|
| `config/pilot-guides.yaml` (оверрайды поведения) | DS-autonomous-agents | версия конфигурации в `assembled_by_run` |
| Окружение прогона: модель генерации текста (LLM-прокси), переменные | tsekh-1 (env) | `assembled_by_run` |

### Б.4 Агрегация происхождения: два слоя, не конфликт

Фрагмент-уровень (10 полей, вводится Ф1) — источник истины происхождения.
Документ-уровень (существующий: `verify_and_provenance.py`, 9 классов входа со статусами
работает/деградировал/запасной/отсутствует) — аудит выхода. Правило: документ-уровень
вычислим из фрагмент-уровня + статусов источников; расхождение слоёв = дефект сборки,
не «два мнения». Маппинг 10 полей → 9 классов документируется при реализации Ф1.
Версия реестра рубрик (§В) на момент сборки фиксируется в `assembled_by_run` — иначе
аудитор не восстановит, какая трактовка `rubric_source` действовала при сборке.

## §В. Происхождение рубрик (закрывает разрыв Н4)

`rubric_ref` обязателен для КАЖДОГО фрагмента ОБОИХ выходов (null запрещён — единый shape
данных; аудитор отличает «рубрика = чеклист выхода» от «пусто = дефект записи»).

Правило происхождения рубрики (по приоритету):
1. Явный критерий качества в самом материале/задании программы → `rubric_source: explicit`.
2. Наследование от типа задания или области → `rubric_source: inherited-from-assignment-type | inherited-from-area`. Наследование — временное и видимое: происхождение рубрики читается по реестру ниже, не маскируется под явное.
3. Ни того ни другого → материал НЕ может быть основным (`role: main`), только поддержкой.

Реестр рубрик (стартовый):

| ID рубрики | Для чего | Чем является сегодня | rubric_source |
|---|---|---|---|
| RUB.GUIDE-OUTPUT-01 | выход «руководство» (все фрагменты) | пара `render_checklist.py` (форма) + `verify_and_provenance.py` (содержание/провенанс) под одним ID | explicit (рубрика выхода; вырожденно для фрагментов — честно) |
| RUB.LESSON.* | основной материал занятия (судья Ф3) | **не существует** — рубрик per-материал в каталогах сегодня ноль | заполнение — обязательная работа Ф6 |

**Честная строка:** рубрик в каталогах материалов сегодня НЕТ (CQR — рубрика авторов другого
каталога `cells/`, заполненных данных 0; deepeval-рубрика — качество бота, не материала).
Наследование по п.2 — переходное. Судья Ф3 не запускается на материале без `rubric_ref`.

## §Г. Аудит машиночитаемости программ-источников (закрывает разрыв Н5)

Критерий допуска материала в `role: main`: поле `prerequisites_checked[]` фрагмента НЕПУСТО —
хоть какая-то машинная проверка готовности/применимости реально выполнена кодом ДО выбора.
Пустой список → только `role: support`. Вид проверки — колонка аудита, не поле фрагмента.

| Источник | Последовательность | Предпосылки | Вид проверки | Как проверяется кодом | Вердикт |
|---|---|---|---|---|---|
| CAT.001-003 | нет (stage-gated, не DAG) | `entry_stage`, `blocks_transition` («2→3») | структурная | guide-kit `planner.py` (bottleneck-first + ступень) | main допустим; `prerequisites_checked: [entry_stage_ok, blocks_transition_ok]` |
| `programs/{lr,rr,ir}/assignments.yaml` | нет | нет; селекция `bottleneck+domain+priority` | селекционная | `assignment_selector.py` (сортировка соответствия) | main допустим; `prerequisites_checked: [bottleneck_match, domain_match]` — **трактовка сессии, см. оговорку** |
| `cells/` (ZP/FPF, шаблон с prerequisites) | шаблон есть, данных 0 | прозаический шаблон | нет (не заполнено) | — | не источник, пока не заполнен |
| decomposer Mode Б (`topic_sections.yaml`) | DAG-структура (prerequisites/depends_on); сортировка — MVP-упрощение, НЕ полный topological sort (оговорка в коде `decomposer.py`) | машиночитаемые | структурная | `guide-kit/generator/decomposer.py` | **резерв**: в суточный конвейер не включён |

**Оговорка (трактовка сессии, требует подтверждения пилота при приёмке Ф0):** допуск
селекционной проверки как «покрытости» — расширительная трактовка ревизии п.5 РП521
(буква: «машиночитаемая последовательность/предпосылки»). Мотив: селекция bottleneck+domain —
машинная проверка применимости ДО выбора; строгая трактовка похоронила бы весь конвейер
МШС-заданий до цифровизации предпосылок, что противоречит решению «не ждать полной
цифровизации». Базовое правило сохранено: ничего не проверено → только поддержка.
Судья Ф3 видит в пакете сам список `prerequisites_checked[]` — знает, оценивает он
цепочку предпосылок или качество эвристического подбора.

## Acceptance (условия смены статусов)

1. Снятие `maturity: draft-pending-F1`: спайк-прогон занятия (нулевой шаг Ф1 РП521)
   подтверждает/корректирует строки `[spike-validated: no]`; пометки снимаются одним коммитом.
2. Контракт конфигураций (§Д карты) живёт черновиком рядом с кодом Портного
   (`DS-autonomous-agents/inbox/selection-config-contract-draft.md`) до реализации
   Ф6/Ф7; после стабилизации — решение о промоции сюда отдельным коммитом с ревью.
3. Изменение состава/порогов фактов выдачи (issued, lesson_closed) — сначала канон РП522,
   потом эта карта (карта — читатель канона, не владелец).
