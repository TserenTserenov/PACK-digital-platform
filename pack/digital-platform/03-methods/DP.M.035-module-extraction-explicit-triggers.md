---
id: DP.M.035
name: Явные триггеры извлечения модуля в сервис
type: method
status: active
valid_from: 2026-05-12
summary: "При выборе 'модуль внутри монолита/Worker' вместо 'отдельный микросервис' — немедленно задокументировать измеримые триггеры обратного extraction. 4 типа триггеров. Без явных триггеров решение становится вечным и пропускает правильный момент для review."
related:
  informs: [DP.ARCH.001]
  see_also: [DP.M.034, DP.SOTA.011]
triggers:
  - "Принято решение оставить логику модулем (не выносить в сервис)"
  - "Написан ADR с вариантом 'модуль внутри'"
  - "IntegrationGate: выбрана реализация как модуль"
created: 2026-05-12
---

# [DP.M.035] Явные триггеры извлечения модуля в сервис

## Суть метода

При принятии решения «оставить как модуль внутри X» вместо «вынести в отдельный сервис» — **немедленно зафиксировать измеримые триггеры**, при которых решение пересматривается. Триггеры пишутся в момент принятия решения, не ретроспективно.

## Forces

_Какие конкурирующие давления удерживает метод._

| Force | Tension |
|-------|---------|
| Скорость закрытия ADR ↔ полнота набора триггеров по всем 4 типам (нагрузочный, безопасный, tenant-граница, размерный) | Под давлением закрыть решение практикующий фиксирует 1-2 очевидных триггера вместо систематического прохода по всем четырём типам таблицы |
| Измеримость порога (конкретное число, напр. «>100 OAuth setup/день») ↔ отсутствие данных о будущем росте на момент принятия решения | Триггер должен быть проверяем без переоткрытия дискуссии, но именно в момент принятия решения «оставить как модуль» реальной нагрузки/роста ещё нет — порог приходится оценивать до данных, не из них |

## Четыре типа триггеров

| Тип | Пример триггера | Когда применять |
|-----|----------------|----------------|
| **Нагрузочный** | `>100 OAuth setup/день в течение 2+ недель` | Когда модуль может стать узким местом при росте |
| **Безопасный** | `Security incident в родительском сервисе` | Когда изоляция критична для blast radius |
| **Tenant-граница** | `Второй tenant/client_id OAuth` | Когда появляются разные security domains |
| **Размерный** | `src/module/ > 5000 LOC` | Когда когнитивная нагрузка превышает разумный порог |

## Почему это важно

**Без явных триггеров** решение «оставить как модуль» становится вечным — команда теряет moment for review. Никто не следит за метриками, решение принято «навсегда».

**С явными триггерами** — кто угодно может проверить: «сработал ли триггер?» без необходимости переоткрывать архитектурную дискуссию с нуля.

## Шаблон в ADR

```markdown
## Триггеры обратного extraction в отдельный сервис

- [ ] >{{N}} {{operation}}/день в течение {{period}}
- [ ] Security incident в {{parent_service}}
- [ ] Появляется {{second_boundary_condition}}
- [ ] {{module_path}}/ > {{LOC}} строк кода
```

## Пример: ADR-IWE-016 (WP-305, 12 мая 2026)

Решение: OAuth логика как модуль `gateway-mcp/src/oauth/` (не отдельный CF Worker).

Зафиксированные триггеры extraction:
- `>100 OAuth setup/день в течение 2+ недель` (нагрузочный)
- `Security incident в gateway-mcp` (безопасный)
- `Второй tenant OAuth` (tenant-граница)
- `gateway-mcp/src/index.ts > 5000 LOC` (размерный)

## Bias-Annotation

_Куда систематически съезжает внимание практикующего._

| Bias | Direction of distortion |
|------|--------------------------|
| Ретроспективная генерация триггеров | «Почему это важно» явно требует писать триггеры в момент решения, не ретроспективно — но внимание практикующего откладывает фиксацию «на потом», полагаясь на память, и в итоге триггеры пишутся пост-фактум или не пишутся вовсе |
| Недооценка размерного триггера | Внимание фокусируется на «интересных» нагрузочном и безопасном триггерах, недооценивая размерный триггер (`> N LOC` из шаблона ADR) — хотя в примере ADR-IWE-016 именно рост LOC в `gateway-mcp/src/index.ts` наиболее вероятен как первый сработавший сигнал |

## Связь с другими методами

- **DP.M.034** (ArchGate Operational Backing): триггеры = операционные данные, которые делают будущий ArchGate сильным
- **DP.SOTA.011** (Coupling Model): triггеры явно связывают coupling-порог с моментом decomposition

---

> 2026-07-27 — миграция на обогащённый формат карточки (Forces + Bias-Annotation), WP-448 Ф12 Батч 3 (суб-батч 2). Эталон формата: `SPF/pack-template/03-methods/_method-card-template.md`.
