---
id: DP.M.439
name: "Сравнение веток по patch-id (git cherry -v), не по числу уникальных SHA"
type: method
pack: PACK-digital-platform
domain: digital-platform / release-engineering
trust: confirmed
epistemic_stage: observed
status: active
valid_from: 2026-09-09
source: "git commit 16ddbeea в aist_bot_newarchitecture (scripts/release-drift-detector.py), WP-562 Ф2, пир-сессия MC-sessions:2026-09/09/2026-09-09-13-wp562-f2-drift-detector"
see_also: [DP.M.414]
schema_version: 1
---

# DP.M.439 — Сравнение веток по patch-id, не по числу уникальных SHA

## Проблема
Наивный подсчёт «сколько коммитов есть в одной ветке и нет в другой» по SHA систематически завышает расхождение между pilot/production ветками, когда практикуется cherry-pick/rebase-перенос: у каждой стороны появляются свои SHA для семантически одного и того же патча. Живой прогон показал 21 SHA-уникальный коммит с одной стороны и 22 с другой при всего ОДНОМ реальном отличии по существу.

## IPO
- **Вход:** две ветки (pilot/production и аналоги), обменивающиеся патчами через cherry-pick/ручной перенос, а не только через общий merge base
- **Процесс:** `git cherry -v` сравнивает по patch-id (хэш нормализованного диффа, не хэш коммита) — схлопывает перенесённые патчи в «уже есть на другой стороне» независимо от SHA. Дополнительно: пометка дельт по чувствительным путям/словам (privacy/auth/gdpr/payment), явная фиксация уже одобренных обходов (`[incident-ok]`/`[cherry-pick-ok]`), append-only журнал соответствий с `first_seen_unmatched_at`, переживающим повторные запуски, алерт только на дельту старше 24ч
- **Выход:** список неэквивалентных патчей в выбранном диапазоне истории, а не число уникальных commit SHA. Это характеристика истории; она не заменяет сравнение конечного состояния веток.

## Граница вывода

Патч и его последующая отмена могут дать то же конечное дерево, что ветка без обоих коммитов, при наличии неэквивалентных патчей в истории. Обратно, эквивалентность выбранных патчей не проверяет отличия за пределами диапазона и рабочую конфигурацию развёрнутой системы. Результаты `git cherry` со знаком `-` означают найденный эквивалентный патч, а не недоставленную работу; учитывать нужно смысл знаков, а не непустоту вывода.

Для вывода «конечные состояния равны» отдельно сравнить итоговые деревья/затронутые пути, включая наличие, типы, режимы и содержимое файлов. Для вывода о работающих системах дополнительно сверить фактически используемую конфигурацию и версии; одинаковое Git-дерево этого не доказывает. Диапазон истории и область сравнения состояния фиксируются явно.

## Применимо
Две ветки/копии одного дерева, обменивающиеся патчами через cherry-pick или ручной перенос (не через общий merge base) — подсчёт по SHA систематически завышает разрыв на порядок именно там, где перенос практикуется штатно.

## Связи
- DP.M.414 (fetch+cherry-pick+retry вместо точного совпадения SHA для публикации) — тот же git-примитив (patch-эквивалентность через cherry-pick), другая ситуация: там — разрешение конфликта публикации одного коммита в реальном времени; здесь — поиск неэквивалентных патчей в заданном диапазоне истории двух веток, дополненный отдельной сверкой конечного состояния.
- Источник: WP-562 Ф2, живой прогон 09.09 (27 боевых хотфиксов не перенесены в pilot, 1 pilot-дельта не перенесена в прод, 9 из 16 чувствительных дельт без override-метки), пир-сессия MC-sessions:2026-09/09/2026-09-09-13-wp562-f2-drift-detector
