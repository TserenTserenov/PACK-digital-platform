---
id: DP.M.036
title: "Онбординг peer-агента в Local Gateway IWE"
type: method
pack: digital-platform
status: draft
trust: 0.8
epistemic_stage: observed
valid_from: 2026-05-13
related: [DP.IWE.005, DP.ROLE.039, DP.SC.035]
---

# DP.M.036 — Онбординг peer-агента в Local Gateway IWE

## Описание

Метод подключения нового peer-агента к Local Gateway IWE (DP.IWE.005). Каждый агент получает отдельную запись в `.mcp.json` с уникальным `IWE_AGENT_ID`, идентифицируется через переменную среды, а не сетевой адрес.

## Forces

_Какие конкурирующие давления удерживает метод._

| Force | Tension |
|-------|---------|
| Уникальность `IWE_AGENT_ID` (изоляция агентов в gateway) ↔ скорость добавления нового агента без пересмотра существующих записей `.mcp.json` | Каждая новая запись должна гарантированно не совпасть с уже занятыми ID, но проверка занятости замедляет то, что задумано как быстрое подключение |
| Полнота 5-шаговой процедуры (расширение → `.mcp.json` → `AGENTS.md` → верификация лога → smoke-test) ↔ желание сразу начать параллельную работу двух агентов | Шаг 5 (smoke-test на разных файлах) — самый дорогой по времени и первый кандидат на пропуск, когда шаги 1-4 уже прошли без видимых ошибок |

## Шаги

1. Установить расширение агента в IDE (пример: moonshot-ai.kimi-code).
2. Добавить запись в `.mcp.json` (IWE root):
   ```json
   "iwe-local-gateway-{agent-id}": {
     "command": "...",
     "env": { "IWE_AGENT_ID": "{agent-id}" }
   }
   ```
3. Создать `AGENTS.md` в корне IWE — инструкции для нового агента (роль, ограничения, контекст).
4. Верифицировать подключение: проверить лог gateway на `agent connected: {agent-id}`.
5. Smoke-test параллельной работы: дать каждому агенту задачу на разных файлах.

## Инварианты

- Каждый peer-агент имеет **уникальный `IWE_AGENT_ID`** — идентификация в gateway через env var, не network address.
- Агенты не делят одну gateway-запись (нет shared entry).
- При конфликте — lock-протокол (DP.SC.035).

## Bias-Annotation

_Куда систематически съезжает внимание практикующего. Статус карточки — `draft`: пометка `tentative` по прецеденту SA.METHOD.001 (WP-448 Ф12)._

| Bias | Direction of distortion |
|------|--------------------------|
| _(tentative)_ Пропуск шага 5 (smoke-test параллельной работы) | Внимание съезжает с проверки реального параллельного сценария (два агента, разные файлы) на формальную проверку шага 4 («agent connected: {agent-id}» в логе) — запись в логе создаёт ложное чувство завершённости onboarding |
| _(tentative)_ Доверие к скопированному `AGENTS.md` | На шаге 3 внимание смещается на переиспользование готового `AGENTS.md` от предыдущего агента вместо адаптации под роль/ограничения именно нового агента — что подрывает саму цель шага (инструкции для НОВОГО агента) |

## Связи

- Сущность: DP.IWE.005 Local Gateway
- Роль: DP.ROLE.039 Peer Agent
- SC: DP.SC.035 Peer Coordination

---

> 2026-07-27 — миграция на обогащённый формат карточки (Forces + Bias-Annotation), WP-448 Ф12 Батч 3 (суб-батч 2). Эталон формата: `SPF/pack-template/03-methods/_method-card-template.md`.
