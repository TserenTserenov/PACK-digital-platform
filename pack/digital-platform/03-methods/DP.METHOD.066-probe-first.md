---
id: DP.METHOD.066
type: method
domain: PACK-digital-platform
status: draft
summary: "Probe-first: read-only разведка live-системы ДО реализации фазы, чья карточка делает проверяемые фактические утверждения о системе."
created: 2026-06-26
valid_from: 2026-06-26
version: v1.0
source: "session-transcript 2026-06-25/26; peer-session 2026-06-25-08 + 2026-06-25-10; WP-439 Ф1+Ф2; git diff DS-MCP/knowledge-mcp (eac18f7, f681001)"
related:
  see_also: [DP.FM.014, DP.ARCH.003]
---

# DP.METHOD.066: Probe-First (разведка ДО реализации)

## Назначение

Метод предотвращения слепой реализации на неверных предпосылках.

Проблема: карточка фазы РП делает конкретные утверждения о live-системе («есть колонка X», «связь означает Y», «N таких узлов»). Эти утверждения могут не соответствовать реальному состоянию системы на момент начала работы.

Решение: запустить read-only разведку (probe) live-системы *до* написания кода и сверить предпосылки.

## Forces

_Какие конкурирующие давления удерживает метод._

| Force | Tension |
|-------|---------|
| Стоимость read-only probe (шаг 2) ↔ скорость старта реализации | SQL/grep/ls разведка до кода откладывает написание кода, но пропуск probe = слепая реализация по карточке, где предпосылки уже опровергнуты фактурой (WP-439: колонка `name_en_source` не существовала, связь `specializes` не вела к синонимам) — DP.FM.014 |
| Полнота выписывания предпосылок (шаг 1) ↔ субъективность границы «проверяемое утверждение» | Метод требует выписать ВСЕ факты, проверяемые grep/SQL, но решение, что считается «проверяемым утверждением», а что общей формулировкой, остаётся на практикующем — недовыписанная предпосылка (как «связь specializes ведёт к синонимам» в Ф2) проходит мимо probe необнаруженной |

## Когда применять

**Триггер:** карточка фазы содержит проверяемые фактические утверждения о live-системе:
- «Колонка `X` существует в таблице `Y`»
- «Связь `specializes` ведёт к синонимам»
- «В этом домене N узлов типа Z»
- «Функция `foo()` принимает параметр `bar`»

**Тест:** «Карточка утверждает факт о системе, который я могу проверить запросом/grep/ls, но не проверил?» → Да → probe-first обязателен.

## Шаги

1. **Идентифицировать предпосылки** — выписать все проверяемые фактические утверждения из карточки фазы.
2. **Запустить read-only probe** — SQL-запросы, grep, ls, curl без side-effects.
3. **Сравнить результат с карточкой** — совпадает / не совпадает.
4. **Если не совпадает → обновить карточку** до начала кода. Слепая реализация по карточке с неверными предпосылками = DP.FM.014 (Legacy Port Jump: прыжок в реализацию без проверки).
5. **Зафиксировать** — числа из probe включить в commit-message или WP-context.

## Применение в WP-439

Обе фазы РП439 были спроектированы на неверной фактуре:
- Ф1 (анкер): карточка ссылалась на колонку `name_en_source` (не существовала) и Part H/K FPF (не было).
- Ф2 (наследование): карточка предполагала, что `specializes` ведёт к синонимам (на данных ведёт к типам-родам — 420 детей у U.Method, имена не наследуются).

В обоих случаях read-only probe боевой системы ДО кода опроверг предпосылки.

## Bias-Annotation

_Куда систематически съезжает внимание практикующего. Статус карточки — `draft`: пометка `tentative`._

| Bias | Direction of distortion |
|------|--------------------------|
| _(tentative)_ Probe как формальность, не как поиск расхождения | При выполнении шага 2 внимание тянется к «запустить нужные команды и получить любой результат», а не к явному шагу 3 «сравнить с карточкой построчно» — практикующий видит вывод probe и неосознанно интерпретирует его в пользу уже написанного плана, вместо того чтобы искать несовпадение |
| _(tentative)_ Фикс кода без фикса карточки | Обнаружив расхождение, внимание смещается сразу к правке реализации, не доходя до шага 4 («обновить карточку ДО начала кода») — карточка остаётся с неверной фактурой для следующего читателя, даже когда код уже написан по факту, а не по устаревшему тексту |

## Связи

- Против паттерна: DP.FM.014 (Legacy Port Jump) — прыжок в реализацию без проверки
- Против паттерна: «Dry-run guard ≠ production run»
- Связан с: [[Код закоммичен ≠ применён к live]], [[«Почти готово» ≠ «Готово»]]

---

> 2026-07-27 — миграция на обогащённый формат карточки (Forces + Bias-Annotation), WP-448 Ф12 Батч 3 (суб-батч 3). Эталон формата: `SPF/pack-template/03-methods/_method-card-template.md`.
