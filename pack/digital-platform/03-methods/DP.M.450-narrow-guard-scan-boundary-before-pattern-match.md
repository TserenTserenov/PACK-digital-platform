---
id: DP.M.450
type: method
title: Сузить границы сопоставления guard-шаблона до реально исполняемых команд — heredoc-тела вырезать, перевод строки считать разделителем
kind: Method
pack: PACK-digital-platform
domain: digital-platform / security-guards
trust: observed
epistemic_stage: confirmed
domains: [security-guard, text-scanning, false-positive, bash-parsing]
source_session: "git diff (несданный) .claude/hooks/destructive-guard.sh (WP-545, 06.09)"
valid_from: 2026-09-06
schema_version: 1
related:
  see_also: [DP.D.296]
---

# DP.M.450 — Сузить сканируемый текст перед сопоставлением с guard-шаблоном, а не добавлять исключения к шаблону

## Определение

Текстовый guard/сканер Bash-команд сужает границы сопоставления самим текстом — что вообще является исполняемой командой — ДО применения проверочного шаблона, вместо накопления точечных исключений к шаблону при каждом новом ложном срабатывании.

## Проблема

Два живых ложных срабатывания одного дня имели общую причину: проверки матчились по сырому тексту Bash-вызова целиком, а не по границам реально исполняемых команд внутри него.

1. Запись документа через `cat > file <<'EOF' ... EOF` была отказана как `rm -rf`, потому что проверка искала `rm` + флаг recursive-ish + флаг force-ish ВЕЗДЕ в тексте вызова, включая тело heredoc (данные, которые записываются, а не команды).
2. `git add` был заблокирован, потому что одиночная точка `source`-команды стояла на ПОЗДНЕЙШЕЙ строке того же вызова, а разделитель сегментов не считал перевод строки границей команды — вызов из нескольких строк склеивался в одну команду.

## Метод

1. **Тело heredoc — данные, не команды.** Вырезать содержимое heredoc из сканируемого текста ДО любой проверки — кроме случая, когда heredoc сам передаётся исполняющему шеллу (тогда его тело и есть список команд, и остаётся в тексте).
2. **Перевод строки — разделитель команд**, наравне с `;`. Без этого любая последующая строка многострочного вызова склеивается с первой командой вызова, и элемент на второй/третьей строке ошибочно матчится как часть первой команды.
3. Применить проверочный шаблон к сокращённому тексту (только реально исполняемые сегменты), не к сырому тексту вызова целиком.

## Обобщение

Переносимый принцип для любого текстового guard/сканера команд: границы сопоставления сужать самим текстом (что вообще является исполняемой командой), а не постфактум городить исключения к каждому конкретному ложному срабатыванию — вторые множатся с каждым новым случаем, первое устраняет целый класс сразу.

## Когда применять

**Тест:** «Guard матчит шаблон по сырому тексту многострочного/составного Bash-вызова целиком?» Да → сузить сканируемый текст до границ исполняемых команд (вырезать heredoc-тела, разделить по переводу строки и `;`) прежде чем добавлять очередное исключение к шаблону.

## Применимость

Любой текстовый guard/сканер, проверяющий Bash-вызовы агента на потенциально опасные конструкции (destructive commands, secret leaks, forbidden patterns) через сопоставление с шаблоном по сырому тексту вызова.

## Связи

- Источник: WP-545 (06.09.2026), `.claude/hooks/destructive-guard.sh`.
- `DP.D.296` (деградирующий парсер ≠ отказывающий) — тот же день и семейство guard-хуков (`secret-bypass-lib.sh`), тот же дух «не блокировать безобидное сразу», но другой механизм: там — классификация типа непонятого ввода; здесь — предварительное сужение текста перед сопоставлением с шаблоном.
