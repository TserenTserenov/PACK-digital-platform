---
id: DP.M.436
type: method
title: "Барьер безопасности в точке правки (post-edit self-test), не только в точке коммита — для общих файлов с мгновенным эффектом"
kind: Method
pack: PACK-digital-platform
domain: digital-platform / методы безопасности
trust: observed
epistemic_stage: confirmed
domains: [security-guard, shared-file, pre-commit, post-edit, defense-in-depth]
source_session: "git commit 20939862 в DS-my-strategy (WP-544 Ф12); feed:git-diff 2026-09-06"
valid_from: 2026-09-06
schema_version: 1
related:
  see_also: [DP.METHOD.182, DP.M.088]
---

# DP.M.436 — Барьер безопасности в точке правки, не только в точке коммита

## Определение

Для общего файла, поломка которого немедленно (до коммита) отражается на всех потребителях — safety-проверка обязана стоять в точке правки (post-edit self-test), а не только в точке коммита. Pre-commit защищает от другого класса риска (публикация сломанного), не от эксплуатации сломанного до публикации.

## Проблема

Файл живёт в незакоммиченном рабочем дереве без синка между параллельными сессиями; правка одного агента становится боевой для ВСЕХ сессий машины за секунды, а до коммита проходят десятки минут. Существующая pre-commit проверка (`bash -n`) физически не может поймать этот класс — она стоит слишком поздно относительно момента, когда ущерб уже нанесён. Замер вечера 06.09 перевёл этот класс из счёта инцидентов в цену простоя: два окна за вечер (34 мин/8 сессий, 68 мин/11 сессий), худшее окно класса — 111 мин/7 сессий.

## IPO-паттерн

| Элемент | Описание |
|---------|----------|
| Входы | Общий файл с несколькими параллельными писателями за день; правка этого файла |
| Обработка | Self-test сразу после сохранения правки (не дожидаясь коммита) — та же проверка, что pre-commit, но триггер — сама правка |
| Выходы | Немедленный сигнал автору правки при поломке — до того, как поломка успела задеть параллельные сессии |

## Тест применимости

«Если этот файл сломается прямо сейчас, до коммита — пострадает кто-то ДРУГОЙ раньше, чем автор закоммитит фикс?» Да → нужен post-edit barrier. Нет (только сам автор пострадает до коммита) → pre-commit/CI достаточно.

## Отличие от смежного

- **DP.METHOD.182 (commit-time hook как shift-left):** аргументирует commit-time точку лучше фонового демона; этот метод идёт ещё левее — для класса «ущерб до коммита» даже commit-time уже поздно.
- **DP.M.088 (CI + pre-commit defense-in-depth):** оба барьера этого метода стоят не раньше коммита; этот метод описывает эмпирически найденный случай (два окна простоя за вечер), где defense-in-depth из DP.M.088 не покрывает risk window между правкой и коммитом.

## Реализация (WP-544 Ф12 мера 1, commit 7c920c3f iwe-local-config)

Принцип «барьер в точке правки» заведён как PostToolUse-хук, а не как очередная инструкция: правило «после правки hook-скрипта проверить синтаксис» три раза проваливалось как памятка в памяти/промпте агента (шесть аварий с 24.08 по 12.09) — агент либо забывал, либо проверял на pre-commit, когда файл уже был боевым для всех параллельных сессий машины. Это promotion E1 → E2 по DP.M.313; триггер — повторный провал правила на уровне инструкции.

Механика:
- **Триггер:** PostToolUse на Edit/Write/MultiEdit; фильтр — путь файла соответствует `.claude/hooks/*.sh`.
- **Проверка:** `bash -n <файл>` + эвристический `--self-test` самого хука; отчёт возвращается в тот же ход агента.
- **Матчинг пути — через `case` по пути относительно `$CWD`, не по суффиксу имени файла.** Суффиксный матч считает `backup/.claude/hooks/x.sh` тем же хуком, что `.claude/hooks/x.sh`, и проверяет/сообщает о файле, который никто не исполняет.
- **Проверяющий хук никогда не блокирует основной поток:** `set -uo pipefail` без `-e`, каждый рискованный вызов — с явным fail-open. Проверка не должна стать новой точкой отказа для всех сессий — иначе лекарство воспроизводит болезнь, от которой лечит.

Граница с DP.FM.412: fail-open — правильное свойство для СОВЕТУЮЩЕГО хука (checker, как здесь и в DP.M.041) и дыра для БЛОКИРУЮЩЕГО (guard). Свойство выбирается по роли хука, не по умолчанию.

Тест применимости promotion: «Правило уже было инструкцией агенту и всё равно нарушалось ≥2 раз с измеримым ущербом?» Да → не четвёртая формулировка памятки, а хук на границе действия (PostToolUse / pre-commit / CI — по этому методу выбирается самая ранняя точка, где ущерб ещё не нанесён).

## Связи

- WP-544 Ф12 (замер простоя: 34 мин/8 сессий + 68 мин/11 сессий + худшее окно 111 мин/7 сессий)
- `DP.METHOD.182`, `DP.M.088` — смежные методы того же семейства (где ставить барьер), ни один не покрывает найденный этим методом risk window
