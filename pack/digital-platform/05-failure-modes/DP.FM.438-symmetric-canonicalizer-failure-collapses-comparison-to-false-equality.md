---
id: DP.FM.438
name: "Непроверенный код возврата канонизатора внутри сравнения: сбой превращается в пустую строку с ОБЕИХ сторон — сравнение молча признаёт совпадение там, где не состоялось вовсе"
type: failure-mode
trust: empirical
epistemic_stage: pattern
status: draft
created: 2026-09-12
sources:
  - "git commit eb3c957a9 (DS-my-strategy, WP-568 Ф2, canon_others_json/canon_all_routes_json, cold-context review после боевой записи)"
related:
  see_also: [DP.FM.040, DP.M.415, DP.D.299, DP.FM.288, DP.FM.266, DP.FM.431]
---

# DP.FM.438: Симметричный отказ канонизатора вырождает сравнение в ложное равенство

## Симптом

Детектор дрейфа «текущее состояние vs каноническое» отвечает «различий нет», хотя сравнение не состоялось: обе стороны перед сопоставлением проходили через внешнюю команду (`jq`) для канонизации, канонизация падала (в таблице пересчёта единиц длительности не было `ms`/`y`), код возврата `jq` не проверялся — переменные на обеих сторонах остались пустыми строками, и `"" == ""` дало «совпадение». Найдено не штатным ревью, а отдельным холодным (cold-context) проходом уже после боевой записи.

## Причина

Значения обеих сторон вычислялись подстановкой `$(canon ...)` в присваивание; код возврата подстановки в такой конструкции теряется (для `local x=$(...)` — всегда; без `set -e`/`pipefail` — и для обычного присваивания), а неудача внешней команды оставляет переменную пустой. Поскольку обе стороны идут через ОДИН И ТОТ ЖЕ отказывающий канонизатор с одним и тем же входом-триггером, сбой синхронен: оба операнда вырождаются в одинаковое значение, и оператор равенства честно сообщает, что они равны.

## Почему это опаснее обычного «тихого exit 0»

Обычный тихий сбой даёт пропуск одной проверки. Здесь сбой маскируется под ПОЛОЖИТЕЛЬНЫЙ результат именно той проверки, ради которой написан скрипт: детектор дрейфа слепнет ровно на тех входах (неизвестная единица, новый формат), где расхождение наиболее вероятно — то есть отказывает избирательно в худшем месте. Ни лог, ни код возврата скрипта, ни результат сравнения не несут признаков сбоя.

## Антипаттерн → Паттерн

- **Антипаттерн:** `a=$(canon "$x"); b=$(canon "$y"); [ "$a" = "$b" ] && echo "no drift"` — код возврата `canon` не проверяется, пустая строка — легитимный операнд.
- **Паттерн:** (1) проверять код возврата каждого канонизатора и прерываться с явной ошибкой (`if ! a=$(canon "$x"); then fail "canon failed: $x"; fi`) — не `|| true`; (2) считать пустой результат канонизации ошибкой, если пустое значение не является валидным каноном для этого типа; (3) канонизатор при неизвестной единице/формате обязан падать громко (fail loud), а не возвращать пустое/исходное значение; (4) вывод «нет расхождений» сопровождать проверкой, что обе стороны непустые и прошли канонизацию успешно — «равны» и «равны, потому что обе не вычислились» должны быть различимы.

## Переносимый принцип

Когда оба операнда сравнения проходят через один и тот же преобразователь (парсер, канонизатор, нормализатор, decrypt), непроверенный отказ этого преобразователя — не безобидная мелочь: он симметрично вырождает оба операнда в одинаковое «пустое» и превращает сравнение в тавтологию. Проверка, которая должна была поймать расхождение, молчит именно тогда, когда преобразователь встретил незнакомый вход — а это и есть типичная форма расхождения.

## Тест обнаружения

«Есть сравнение, обе стороны которого вычисляются одной внешней командой/функцией, и её код возврата или пустой результат не проверяются перед сравнением?» Да → подать на вход значение, которое преобразователь заведомо не понимает (неизвестная единица, новый формат): если детектор отвечает «совпадает / нет дрейфа» вместо ошибки — DP.FM.438.

## Применимость

Drift-детекторы desired-vs-actual, idempotent provisioning, сравнение конфигов после нормализации, сверка контрольных сумм/канонических JSON, любые shell-сравнения, где операнды получаются через `$(...)`.

## Отличие от смежного

- **DP.FM.040 (silent-null parser):** парсер возвращает пустое на нераспознанный синтаксис, и пустое ПРОХОДИТ валидацию — односторонний механизм; здесь пустое возникает на обеих сторонах равенства, и ложным становится не «пропуск», а «положительное совпадение».
- **DP.M.415 (захват реального кода возврата вместо `|| true`):** антидот для явного подавления ошибки одного защитного шага; здесь код теряется неявно через подстановку в присваивании, и ущерб — в семантике сравнения, а не в пропущенном шаге.
- **DP.D.299 (код возврата конвейера — только последняя команда):** другой источник потери статуса (pipe), тот же класс «статус потерян до проверки».
- **DP.FM.288 (0 итераций → exit 0), DP.FM.266 (exit 0 ≠ структурная корректность):** тихий успех при отсутствии работы; здесь работа как бы сделана и результат как бы положительный.
- **DP.FM.431 (форма стороннего контракта предположена):** тот же коммит — канонизация длительностей введена как лечение нормализации сервером при записи; этот FM — о том, как само лечение отказывает молча.

## Связанные документы
- Источник: WP-568 Ф2, commit eb3c957a9 (DS-my-strategy), cold-context review
- see also: DP.FM.040, DP.M.415, DP.D.299, DP.FM.288, DP.FM.266, DP.FM.431
