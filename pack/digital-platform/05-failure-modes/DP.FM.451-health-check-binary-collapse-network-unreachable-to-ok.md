---
id: DP.FM.451
type: failure-mode
status: active
created: "2026-09-14"
valid_from: "2026-09-14"
name: "Health-check с бинарным исходом молча схлопывает «не смог проверить» в «всё в порядке» при недоступности сети"
name_ru: "Health-check с бинарным исходом молча схлопывает «не смог проверить» в «всё в порядке» при недоступности сети"
name_en: "A binary-outcome health-check silently collapses \"couldn't check\" into \"all clear\" when the network is unreachable"
summary: "Функция проверки отставания от origin возвращала «ok» и при реальном «нет расхождения», и при невозможности вообще выполнить fetch (нет сети) — двухзначный статус защитной проверки не различает эти случаи, поэтому сбой самой проверки маскируется под её положительный вердикт."
pack: PACK-digital-platform
domain: digital-platform
schema_version: 1
trust: medium
epistemic_stage: forming
source: "session-transcript 2026-09-14 (пир-сессия 2026-09-14-08-wp484-deliver-sentinel-fix-sync-alerting, cold-review субагента) + git diff корневой репо e3f034b4da, day-open-preflight.sh check_repo_drift"
extraction_report: "DS-my-strategy/inbox/extraction-reports/2026-09-16-inbox-check-3.md"
source_candidate: 1
source_capture: "DS-my-strategy/inbox/captures/2026-09.md:1874"
related:
  see_also: ["DP.FM.438", "DP.FM.220", "DP.FM.418", "PACK-agent-rules:AR.286"]
---

# DP.FM.451 — Health-check с бинарным исходом схлопывает «не смог проверить» в «всё в порядке»

## Тезис и механизм ошибки

Проверка отставания от origin (`check_repo_drift`) была добавлена как независимый от Telegram канал наблюдения за тем же фактом. Её первая версия сама повторила класс дефекта, который должна была страховать: функция выполняла `git fetch` и сравнивала с origin, возвращая «ok» и тогда, когда расхождения нет, и тогда, когда `fetch` не удался из-за отсутствия сети. Оба случая давали на выходе один и тот же положительный статус — отсутствие сети физически не позволяет узнать реальное состояние, но проверка отвечала так, будто состояние известно и благополучно.

## Диагностический принцип

У любой защитной проверки, дающей внешнему наблюдателю один статус, минимум три содержательных исхода: «ок» (проверено, проблемы нет), «проблема» (проверено, проблема есть), «не смог проверить» (сама проверка не выполнилась). Третий исход не должен схлопываться в первый — сбой инструмента проверки (нет сети, нет доступа, таймаут) не есть доказательство благополучия объекта проверки.

## Границы

Различение не требует отдельного явного three-valued API у каждой проверки без исключения — там, где сама проверка не имеет внешних зависимостей в пути выполнения (не может физически «не выполниться» независимо от объекта), двузначный статус безопасен. Правило применимо именно там, где проверка зависит от внешнего ресурса (сеть, файл, процесс), который может быть недоступен независимо от состояния объекта проверки.

## Проверка

Тест-сценарий «офлайн» (отключённая сеть/недоступный внешний ресурс) обязателен в smoke-наборе любой защитной проверки. «Пропустить fetch/запрос через проверку, эмулируя недоступность сети — возвращает ли она нейтральный „не смог проверить“, отличимый от „ok“?» Нет → воспроизведён DP.FM.451.

## Происхождение и статус проверки

Первичный источник: session-transcript 2026-09-14 (пир-сессия 2026-09-14-08-wp484-deliver-sentinel-fix-sync-alerting, cold-review субагента) + git diff корневого репо `e3f034b4da` (`day-open-preflight.sh`, функция `check_repo_drift`). Захват: `DS-my-strategy/inbox/captures/2026-09.md:1874`. Связь: WP-484, `day-open-preflight-sync-drift-smoke.sh`.

Смежно, не дубль: `DP.FM.438` (симметричный отказ канонизатора вырождает СРАВНЕНИЕ двух значений в ложное равенство — здесь единственная проверка сама даёт двузначный статус, без сравнения двух вычисленных величин); `DP.FM.220` (health-check ложно ОТКАЗЫВАЕТ из-за побочного эффекта DDL под нагрузкой — противоположное направление коллапса: там сбой маскируется под отказ, здесь под успех); `DP.FM.418` (сообщение об отказе guard'а не отличает «сам guard сломан» от «команда отклонена» — тот же корневой класс «сбой инструмента проверки неотличим от его вердикта», но там оба неразличимых исхода отрицательные, здесь один из них — ложноположительный); `PACK-agent-rules:AR.286` (проверка внешнего компонента различает fail/disabled/неопределено — тот же принцип «не менее трёх состояний», для другой оси: там разные причины отсутствия компонента, здесь разные исходы попытки проверить).

Оставленный долг (зафиксирован для следующего цикла, не формализован отдельной карточкой): проверка жёстко называет ветку `main` вместо резолва дефолтной — тот же класс маскировки повторится при переименовании ветки по умолчанию.

Описанная проверка задаёт критерий приёмки. Её прохождение исходной реализацией этой карточкой не утверждается.
