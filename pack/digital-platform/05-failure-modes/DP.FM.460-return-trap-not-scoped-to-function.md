---
id: DP.FM.460
type: failure-mode
status: active
created: "2026-09-15"
valid_from: "2026-09-15"
name: "`trap ... RETURN` внутри bash-функции не привязана к границе этой функции — переживает её и срабатывает повторно на следующем return"
name_ru: "`trap ... RETURN` внутри bash-функции не привязана к границе этой функции — переживает её и срабатывает повторно на следующем return"
name_en: "A `trap ... RETURN` set inside a bash function is not scoped to that function — it outlives it and fires again on the next return anywhere"
summary: "Функция ставила `trap ... RETURN`, ссылаясь на свои локальные переменные, в расчёте на срабатывание один раз при выходе именно из неё. В bash trap RETURN не имеет такой границы — он остаётся активным глобально и срабатывает на возврате из ЛЮБОЙ последующей функции, включая выполняемые намного позже в другом вызове хука."
pack: PACK-digital-platform
domain: digital-platform
schema_version: 1
trust: medium
epistemic_stage: forming
source: "git commit 4560d80 в FMT-exocortex-template (issue #818)"
source_capture: "DS-my-strategy/inbox/captures/2026-09.md:2086"
related:
  see_also: []
---

# DP.FM.460 — `trap RETURN` в bash не ограничена функцией, в которой поставлена

## Тезис и механизм ошибки

Функция ставила `trap ... RETURN`, ссылаясь на свои локальные переменные, в расчёте, что ловушка сработает один раз при выходе именно из неё. В bash `trap RETURN` не имеет такой границы — он остаётся активным глобально и срабатывает на возврате из ЛЮБОЙ последующей функции, включая те, что выполняются намного позже в другом вызове хука. В штатном сценарии без активной репетиции это привело к падению «unbound variable», когда тот же хук позже сорсил не связанный с этой функцией файл, и оставленная ловушка сработала повторно, ссылаясь на уже недоступные локальные переменные исходной функции.

## Диагностический принцип

`trap ... RETURN`, поставленный внутри функции bash, — не «функциональная» ловушка с автоматической границей действия, а глобальная ловушка с отложенным и неожиданным местом срабатывания, если её явно не снять. Предположение «ловушка сработает и исчезнет при выходе из моей функции» неверно для bash и требует явной очистки.

## Границы

Различение специфично для bash (в других шеллах поведение `trap RETURN` может отличаться) и касается именно `RETURN`-ловушек, ссылающихся на локальные переменные или состояние конкретной функции. Глобальные ловушки, спроектированные как глобальные с самого начала, вне scope этой находки.

## Проверка

«Функция ставит `trap ... RETURN` и не снимает её явно перед собственным `return`?» Да → воспроизведён DP.FM.460: ловушка переживёт функцию и сработает на возврате из следующей функции где угодно в процессе. Фикс — явно снимать trap перед собственным `return` функции.

## Происхождение и статус проверки

Источник: git commit 4560d80 в FMT-exocortex-template (issue #818). Захват: `DS-my-strategy/inbox/captures/2026-09.md:2086`. Связь: WP-7, PR #828, peer-session 2026-09-15-02-fmt-issues-triage-wp7.

Описанная проверка задаёт критерий приёмки. Её прохождение исходной реализацией этой карточкой не утверждается.
