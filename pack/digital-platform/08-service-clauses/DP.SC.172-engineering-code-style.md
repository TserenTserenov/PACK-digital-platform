---
id: DP.SC.172
name: База инженерного стиля кода
type: sc
status: active
layer: L4-Personal
summary: "Агент-разработчик выдаёт код craft-уровня (без перечисленных запахов) при написании кода в репозиториях IWE"
consumer: R01 Пилот (как ревьюер кода), будущие сопровождающие, агенты-исполнители
created: 2026-06-10
valid_from: 2026-06-10
related:
  realizes: []
  uses: [DP.SC.050]   # тот же паттерн «база стиля + слои L0/L1/L2 + хук инжекции + детектор + аггрегатор»
  extends: []
---

# [DP.SC.172] База инженерного стиля кода

## Правило (инвариант)
- [x] Один источник истины L0 в PACK-digital-platform (`02-domain-entities/engineering-code-style-base.md`), три слоя L0/L1/L2.
- [x] Перед написанием кода правила P0-P6 загружены в контекст агента (хук инжекции PreToolUse + якорь UserPromptSubmit).
- [x] Каждое правило сводимо к паре «было/стало»; не сводимое — не в L0.
- [x] Граница с PACK-agent-rules (AR.*) соблюдена: блокирующие запреты — там, craft-слой — здесь.

## Обещание
- **Кому:** R01 Пилот как ревьюер; будущие сопровождающие кода; агенты-исполнители.
- **Зачем:** код агента читается профессионалом как профессиональный, а не «писала машина».
- **Что получит:** единый craft-стандарт от всех агентов (Claude/Kimi/Hermes) + авторские надстройки L1 + пользовательские L2.
- **Триггер:** агент собирается писать/править код (первый code-touch сессии / Edit-Write по code-файлу).
- **Время отклика:** немедленно (загружено в контекст до генерации кода).
- **Режим отказа:** агент работает по последней загруженной версии; drift детектируется страховочным скриптом `code-style-hook.sh` и (v2) review-агентом.

## Свидетельства (4 компонента семиозиса)
- **Данные:** L0-файл в Pack, L1-файл автора, три хука в settings.json, детектор-скрипт, аггрегатор.
- **Контекст:** агент пишет код (детектор контекста — «есть будущий читатель?»), хук сработал перед Edit/Write.
- **Полномочия:** автор платформы (L0), пилот (L1), детектор-скрипт + review-агент v2 (нарушения).
- **Свидетельства:** лог детектора `~/.claude/logs/style-violations.log` (правила P1/P2/P4), отчёт review-агента (v2), аудит diff на Week/Month Close.

## Сценарии использования (≥3)
1. **Claude пишет новый сервис** — хук впрыскивает P0-P6; агент не плодит абстракции, гоняет тесты с реальными assert, запускает линтер репо (P0).
2. **Kimi правит существующий модуль headless** — без доступа к pre-commit hook P0 заставляет запустить форматтер вручную; детектор-скрипт ловит `except: pass`.
3. **Пилот ревьюит PR агента** — видит код без знакомых запахов; расхождения с базой = быстрый review-комментарий со ссылкой на P-номер.

## Связь с другими обещаниями
- Потребляет: DP.SC.050 (тот же слоёвый паттерн, механизм инжекции, единый лог нарушений и аггрегатор `style-feedback-loop.py`).
- Используется в: процесс ревью/мёржа (станция «Проверка» конвейера WP-403, DP.SC.171).

## Реализация (WP-408 Ф3)
- L0: `02-domain-entities/engineering-code-style-base.md` (маркеры `CODE-STYLE-INJECT-START/END`, ядро ~6.8к символов).
- L1-слот: `DS-my-strategy/memory/code-style-author.md` (контракт additive-only, правил пока нет).
- Хуки: `inject-code-style-anchor.sh` (UserPromptSubmit, stateless), `inject-code-style.sh` (PreToolUse Edit|Write, кап 9500), `code-style-hook.sh` (Stop, `git diff HEAD`, P1/P2/P4).
- Аггрегатор: `style-feedback-loop.py` (обе оси — A-правила текст + P-правила код).
- Проекция Kimi: SYNC-CORE → AGENTS.md. Hermes: follow-up.

## Доставка (через единый реестр стилей, WP-408 Ф5 + WP-412)

> С WP-412 (система управления языковыми стилями) доставка P0-P6 к агенту идёт через **единый реестр**, а не через standalone-хук. Эта SC остаётся **источником истины контента** (`content_source`), не становится потребителем правил.

- **Регистрация:** P0-P6 представлены в реестре регистром `LS.FORM.002-register-code-developer` (`PACK-rhetoric/pack/language-style/registry/`). Это **слот-указатель** (`content_source: DP.SC.172`, `projection: read-only`), не копия — компилятор тянет живой контент из L0-базы. Регистрация ссылочная: владелец контента остаётся PACK-digital-platform (эта SC + `engineering-code-style-base.md`).
- **Путь доставки:** диспетчер реестра (`registry/dispatcher.py --event pretooluse-edit`) → компилятор → фрагмент в контекст агента. Хук `inject-code-style.sh` вызывает диспетчер (не хардкодит правила).
- **Режим отказа реестра (file-fallback):** при недоступности реестра хук падает на статический снимок `.claude/styles/p05-core.md` (промоция WP-412 Ф11). Установки без PACK-rhetoric получают ядро P0-P6 из снимка.
- **Граница владения:** контент P0-P6 = WP-408 (PACK-digital-platform); инфраструктура доставки (реестр, компилятор, диспетчер, хук-вызов, file-fallback) = WP-412. Один факт — одно место.
