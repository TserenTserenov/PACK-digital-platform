---
id: DP.SC.199
name: agent-autonomy-progression
type: sc
status: draft
layer: L4-Personal
summary: "Автономность агента на класс задач растёт по измеримому track record успешных закрытых циклов, не выдаётся одним актом; провал/откат сужает scope автоматически — механизм не спроектирован, только скелет обещания"
consumer: R01 Пилот
created: 2026-07-27
updated: 2026-08-09
related:
  realizes: []
  uses: [DP.SC.037]
  extends: []
wp: WP-507
---

# DP.SC.199 — Прогрессирующая автономность агента с откатом

> **Статус: скелет, не проектный документ.** Реализует принцип #34 (DP.ARCH.001 §7), но сам принцип был сформулирован сегодня (27.07) в брейншторме направлений развития IWE — механизм ниже НЕ откалиброван и НЕ проверен практикой. Открытые вопросы явно перечислены и требуют решения пилота до перевода в `status: active`.

## Правило (инвариант)

> Что ВСЕГДА должно выполняться для данного сервиса. Нарушение = провал SC.

- [ ] Уровень автономии агента (сколько шагов/решений без подтверждения) присваивается **по классу задачи**, не глобально — рост в одном классе не переносится на другой.
- [ ] Рост уровня — только по накопленному track record (N успешных закрытых циклов подряд без отката пилотом) для данного класса.
- [ ] Провал/откат пилотом на текущем уровне — немедленное сужение scope до предыдущего проверенного уровня, не «попробовать ещё раз на том же уровне».
- [ ] История изменения уровня (когда повышен/понижен, из-за чего) трассируется, не теряется.

## Обещание

**Кому:** R01 Пилот

**Зачем:** расширение автономии агента («пилот как начальник команды агентов» — направление развития IWE от 27.07) не должно быть актом веры один раз — оно должно расти вместе с фактическим доверием и откатываться при сбое без ручного пересмотра всей политики.

**Что получит:** видимый, объяснимый уровень автономии на класс задачи + автоматическую защиту от продолжения на уровне, переставшем быть надёжным.

**Триггер:** агент завершает цикл задачи данного класса (успешно или с откатом пилотом).

**Время отклика:** обновление уровня — на закрытии цикла, не отложенно.

**Режим отказа:** нет данных по классу задачи → минимальный уровень автономии (максимум подтверждений) по умолчанию, не оптимистичный.

## Открытые вопросы (блокируют `status: active`)

| # | Вопрос | Почему не решено сегодня | Предложение (пир-сессия 30.07, черновик) |
|---|--------|---------------------------|-------------------------------------------|
| 1 | Как определяется «класс задачи» — по типу WP, по репо, по `verification_class`? | Не предметная область Claude в этом Pack — решение пилота | **Черновик 09.08:** класс = `verification_class` (trivial / closed-loop / open-loop / problem-framing) + короткий список репо с замороженной автономией. Основание: поле уже обязательно в frontmatter каждой карточки РП и объявляется в Ритуале согласования, то есть класс известен ДО начала работы и не требует нового реестра; ось «неоднозначность» — ровно та, что определяет, сколько может пойти не так незаметно. Отвергнуто: класс по репо (30+ репо → track record разрежен, N=2-4 не набирается; репо отражает цену ошибки, а не неоднозначность), класс по типу РП/скиллу (дробление до единичных наблюдений). Компенсация слепого пятна «тривиальная правка в дорогом репо»: не вторая ось, а стоп-список репо (`payment-registry`, прод-боты, read-only) — в них уровень не растёт независимо от track record. Черновик, решение за пилотом. |
| 2 | Порог N (сколько успешных циклов для повышения уровня)? | Не откалиброван, нет исторических данных | Условно на решении #1: N=2 для routine/low-ambiguity класса, N=3 по умолчанию, N=4-5 для complex/high-ambiguity; пересмотр порогов после первых 10 реальных переходов уровня по факту (сколько ложных повышений/откатов дал стартовый набор). Черновик, не финал — требует реальных данных. |
| 3 | Что считается «откатом» — явное «нет, не так», отменённый коммит, баг-репорт постфактум? | Разные сигналы дают разную скорость реакции | Трёхуровневая шкала вместо бинарной: явное «нет, не так» → немедленное сужение (максимальная достоверность). Отменённый коммит без комментария → агент обязан задать пилоту follow-up-вопрос в начале следующей сессии («вижу откат X — качество решения или независимая причина?»); нет ответа за N циклов → засчитывается как откат по качеству. Баг-репорт постфактум → засчитывается в следующее окно N, не откатывает уже пройденные циклы задним числом. |
| 4 | Где хранится текущий уровень автономии (Neon / memory / отдельный yaml)? | Архитектурное решение вне узкого объёма WP-507 | **Черновик 09.08:** источник истины — версионируемый yaml `DS-my-strategy/machine/agent-autonomy-levels.yaml` (рядом со `strategic-context.yaml` — тот же класс «машинное производное, не читаемое пилотом»); Neon (`domain_event`) — производный поток для дашборда, не источник. Три основания: (1) инвариант «история изменения уровня трассируется» закрывается git-историей бесплатно и атомарно с самим изменением, отдельный журнал не нужен; (2) гейт автономии срабатывает ДО первого сетевого вызова — локальный файл читается всегда, Neon в оффлайне/на другом хосте нет, а fail-safe «нет данных → минимальный уровень» тогда срабатывал бы от сетевой ошибки, а не от отсутствия track record; (3) `memory/*.md` отвергнута — бюджет ≤11 файлов, немашинный формат, и агент правил бы собственные права в файле, который сам же грузит в контекст. **Открытый под-вопрос (конфликт интересов):** писатель уровня = тот, кому уровень выгоден. Предлагаемая защита — запись только детерминированным скриптом на закрытии цикла (reflex, без LLM-решения) + проверка в хуке, что дельта = ±1 уровень и ссылается на trace DP.SC.037. Черновик, решение за пилотом. |

## Сценарии использования (≥3, иллюстративные — механизм не спроектирован)

1. Агент 10 раз подряд успешно закрывает тривиальные (`verification_class: trivial`) РП без отката пилотом → получает право пропускать явное подтверждение для этого класса.
2. Агент ошибается в open-loop задаче (пилот откатывает решение) → уровень автономии для open-loop сужается до предыдущего, требует больше подтверждений на следующих попытках.
3. Новый класс задачи (например, работа с payment-registry) стартует с нулевого уровня автономии независимо от track record в других классах.

## Связь с другими обещаниями

- Использует: **DP.SC.037** (agent-trace) как источник track record — без trace нет данных для подсчёта успешных циклов.
- Не путать с **DP.SC.185** (tier-authority) — тот про уровни доступа T1-T4 команды людей, этот — про автономию самого агента.
- Смежный: **DP.SC.198** (agent-action-reversibility) — откат уровня автономии здесь ссылается на тот же класс событий, что и отмена действия там, но это разные оси (превентивный гейт vs накопительный track record).
