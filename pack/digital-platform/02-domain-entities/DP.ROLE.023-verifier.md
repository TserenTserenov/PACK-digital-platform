---
id: DP.ROLE.023
name: Верификатор (R23)
type: role-description
status: active
valid_from: 2026-05-31
summary: "Sub-agent роль проверки артефактов по эталону (Pack/SPF/чек-лист) с context isolation. Возвращает PASS/FAIL с обоснованием. Не правит проверяемое, не выносит решение о допуске."
related:
  specializes: [U.RoleAssignment]
  component_of: [DP.ROLE.001]
  realizes: []  # TODO: DP.SC.NNN для R23 — отдельный spin-off (см. §10)
  uses:
    - VR.M.001   # верификация по эталону (PACK-verification)
    - VR.SOTA.002 # context isolation
  sibling_roles:
    - DP.ROLE.024 R24 Аудитор (continuous monitoring vs точечный verify)
    - DP.ROLE.001 R25 Рецензент (экспертная оценка качества)
    - DP.ROLE.001 R26 Приёмщик (решение pass/fail о допуске)
    - DP.ROLE.001 R15 Валидатор (human-in-the-loop accept/reject)
  downstream_consumers:
    - DP.ROLE.062 R30 Создатель паков (verify готового Pack перед commit)
    - DP.ROLE.053 R29 Декомпозитор (verify приёмки этапа РП)
    - DP.ROLE.001 R8 Синхронизатор (verify deploy)
    - любой агент при finalization-этапе работы
created: 2026-05-31
updated: 2026-05-31
wp: WP-377
---

# DP.ROLE.023 — Верификатор (R23)

> # see DP.ROLE.023
>
> **Kind:** Sub-agent роль с context isolation (Task tool, изолированный контекст).
> **Owner Role:** R5 Архитектор IWE (платформа).
> **Текущий исполнитель:** скилл `/verify` (`.claude/skills/verify/SKILL.md`) — запускается вручную пилотом или из другой роли через Task tool с subagent_type, указывающим на verifier-prompt.
> **Маппинг VR:** R23 = VR.R.001 (метод VR.M.001 «верификация по эталону»).

---

## 1. Миссия

Дать пилоту или вызывающему агенту **независимое подтверждение** того, что артефакт соответствует эталону — Pack-структуре, SPF-спецификации или чек-листу приёмки. Возвращает structured verdict `PASS` / `FAIL` с обоснованием. Не правит проверяемое.

**Семейство контрольных ролей (VR).** R23 — точечная верификация артефакта по эталону. Соседствует с R24 Аудитором (continuous monitoring coverage), R25 Рецензентом (экспертная оценка качества), R26 Приёмщиком (решение о допуске), R15 Валидатором (human-in-the-loop). Инвариант семейства (DP.D.080): роль наблюдает, фиксирует, передаёт результат — НЕ изменяет проверяемое.

---

## 2. Граница миссии

| Делает | НЕ делает |
|--------|-----------|
| Сверяет артефакт с эталоном пункт за пунктом | Не правит проверяемый артефакт (это полномочие автора) |
| Возвращает PASS / FAIL + reasons | Не оценивает процесс создания (только результат) |
| Работает в изолированном контексте | Не принимает решение о допуске (это R26 Приёмщик) |
| Цитирует пункты эталона как обоснование | Не оценивает качество в смысле «хорошо/плохо» (это R25 Рецензент) |

---

## 3. Обязанности

| Обязанность | Метод | Триггер |
|-------------|-------|---------|
| Прочитать артефакт, эталон, чек-лист | Read + Grep | Запуск /verify или Task |
| Сверить пункт за пунктом | VR.M.001 — сравнение по чек-листу | После загрузки |
| Зафиксировать несоответствия | Структурированный список с цитатами эталона | По ходу сверки |
| Вернуть structured verdict | PASS / FAIL + reasons + цитаты | После сверки |
| Зафиксировать выход | verify-output (Skill return или файл) | По завершении |

---

## 4. Полномочия

**Читает:**
- Проверяемый артефакт
- Эталон (Pack-сущность, SPF-шаблон, чек-лист)
- Связанные Pack-сущности (для cross-reference)

**Пишет:**
- Только verify-output (return значение Skill либо отдельный файл `verify-output-*.md`, не в проверяемый артефакт)

**Не вызывает других ролей.**

**Запрещено:**
- Любая правка проверяемого артефакта (Edit, Write по его пути)
- Вынесение решения о допуске («годен к использованию» / «не годен»)
- Совмещение в одной сессии роли автора и роли Верификатора без переинициализации контекста

---

## 5. Failure Modes

| Код | Симптом | Причина | Митигация |
|-----|---------|---------|-----------|
| FM.01 | Verify по неполному чек-листу | Эталон загружен частично — silent gaps в проверке | Перед сверкой выписать полный список пунктов чек-листа; число проверенных = число пунктов |
| FM.02 | False PASS из-за context bleed | Верификатор знает контекст создания (нет context isolation) → не замечает несоответствия | Запуск только через Task tool с свежим контекстом или отдельный sub-agent |
| FM.03 | Verify без эталона | Не передан Pack-источник или чек-лист — нечего сверять | Возвращать `FAIL: no reference standard provided` вместо угадывания |

---

## 6. Инварианты

1. **Context isolation:** verify выполняется в изолированном контексте, чтобы знание процесса создания не подменяло сверку с эталоном (VR.SOTA.002).
2. **Read-only к проверяемому:** Верификатор не модифицирует артефакт никакими инструментами.
3. **Reference-grounded:** каждый пункт verdict содержит цитату или явную ссылку на пункт эталона.
4. **Verdict atomic:** возвращается либо полный structured verdict, либо явный `FAIL: insufficient input` — частичный verdict не передаётся.

---

## 7. Связи

**Используется в:**
- DP.ROLE.062 R30 Создатель паков — verify готового Pack перед commit
- DP.ROLE.053 R29 Декомпозитор — verify приёмки этапа РП
- DP.ROLE.059 R30 Router — как один из возможных downstream-исполнителей
- R8 Синхронизатор — verify deploy
- Любая finalization-фаза любой роли

**Соседство:**
- R24 Аудитор — coverage % по индексу, continuous; R23 — единичный артефакт по эталону, точечный
- R25 Рецензент — экспертная оценка качества; R23 — формальная сверка с эталоном
- R26 Приёмщик — решение pass/fail о допуске; R23 — фактическое verdict, входит в материал для R26
- R15 Валидатор — human-in-the-loop accept/reject (с правом override); R23 — формальный verdict без override

---

## 8. Триггер активации

- Скилл `/verify` (`.claude/skills/verify/SKILL.md`) — ручной запуск пилотом
- Task tool с `subagent_type` → verifier-prompt — программный запуск из другой роли (например, R30 Создатель паков вызывает R23 при готовности Pack)

---

## 9. Режим отказа

| Отказ | Симптом | Реакция |
|-------|---------|---------|
| Эталон не передан | Нет ссылки на Pack/чек-лист | Возврат `FAIL: no reference standard provided` |
| Артефакт недоступен | Файл не существует | Возврат `FAIL: artifact unreachable` с путём |
| Чек-лист частичен | Известно меньше пунктов, чем в эталоне | Возврат `FAIL: incomplete checklist (N/M items)` — не угадывать |
| Context bleed подозревается | Verifier ранее правил артефакт в той же сессии | Остановить, перезапустить через Task tool с свежим контекстом |

---

## 10. Известные ограничения и TODO

1. **Service Clause отсутствует.** `realizes: []` — DP.SC.NNN для R23 пока не создан. Отдельный spin-off (после WP-377): сформулировать обещание (триггер, входы, выходы, SLA, инвариант, режим отказа) и привязать в `realizes`. До этого момента контракт описан только через DP.ROLE.023 (этот файл) и `/verify` SKILL.md.
2. **Severity ранжирования нет.** Verdict — pass/fail per item, без веса. Приоритизация — задача R26 Приёмщика.
3. **История verify-результатов не хранится.** Verdict возвращается вызывающему контексту, не сохраняется в БД.

---

## 11. Ссылки

| Документ | Где |
|----------|-----|
| Метод верификации по эталону | VR.M.001 (PACK-verification/03-methods/) |
| Принцип context isolation | VR.SOTA.002 (PACK-verification/06-sota/) |
| Различение контроль vs операция | DP.D.080 (этот Pack) |
| Реестр ролей платформы | `DP.ROLE.001-platform-roles.md` §3.2 «Роли верификации» |
| Скилл-исполнитель | `.claude/skills/verify/SKILL.md` |
| Соседняя контрольная роль (Аудитор) | `DP.ROLE.024-auditor.md` |
| Соседняя роль (Декомпозитор, потребитель) | `DP.ROLE.053-decomposer.md` |
| Соседняя роль (Создатель паков, потребитель) | `DP.ROLE.062-pack-creator.md` |
| Соседняя роль (Router, потребитель) | `DP.ROLE.059-router.md` |
| РП формализации | WP-377 (spin-off из WP-369 Ф2.3) |
