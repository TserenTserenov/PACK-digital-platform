---
id: DP.IWE.011
name: IWE Runtime Host Contract
name_ru: Контракт хоста для IWE
type: domain-entity
status: draft
layer: L4-Distribution
version: 0.1.0
created: 2026-05-21
related:
  specializes: DP.IWE.001
  referenced_by: [DP.SC.046, DP.SC.019]
  related: [DP.IWE.003, DP.IWE.005]
source: WP-337 направление-Ж (стратсессия 21 мая 2026)
---

# DP.IWE.011 — Контракт хоста для IWE

> IWE = дистрибутив (слой 4). Для работы он требует от хоста (слой 3) выполнения минимального контракта.
> Этот документ описывает **что любой хост ОБЯЗАН предоставить**, чтобы ОРЗ-протоколы IWE работали без изменений.

---

## 1. Назначение контракта

**Проблема:** Сейчас протоколы open/work/close написаны под Claude Code напрямую. При смене хоста (CC → Hermes → бот) протоколы ломаются, потому что используют конкретные инструменты (TodoWrite, ScheduleWakeup), хук-события (PostToolUse/Stop) и переменные окружения (CLAUDE_TASK_ID).

**Решение:** Специфицировать минимальный HOST CONTRACT — абстрактный интерфейс, который любой хост реализует в своём адаптере. IWE-протоколы пишутся против контракта, не против конкретного хоста.

**Аналогия:** POSIX для Unix. Каждый хост реализует POSIX-совместимый слой; программы (IWE-протоколы) работают на любом POSIX-хосте.

---

## 2. Обязательный интерфейс (HOST CONTRACT v0.1)

### A. Tool API — инструменты пилота

Хост ОБЯЗАН предоставить следующие инструменты или их функциональные эквиваленты:

| Инструмент | Назначение | Текущая реализация (CC) | Требования к эквиваленту |
|------------|-----------|------------------------|--------------------------|
| `task_tracker.create(tasks[])` | Создать список задач | `TodoWrite` с массивом объектов | Атомарно создаёт N задач; возвращает список ID |
| `task_tracker.update(id, status)` | Обновить статус задачи | `TodoWrite` с обновлёнными полями | `status ∈ {pending, in_progress, completed}` |
| `task_tracker.list()` | Получить текущий список | `TodoWrite` (чтение) | Возвращает все незакрытые задачи |
| `scheduler.schedule_at(ts, msg)` | Запланировать напоминание | `ScheduleWakeup` | Доставляет сообщение в канал пилота в момент `ts` |
| `fs.read(path)` | Читать файл | `Read` | Возвращает содержимое файла; ошибка если не существует |
| `fs.write(path, content)` | Записать файл | `Write` | Перезаписывает целиком; создаёт директории |
| `fs.edit(path, old, new)` | Точечная правка файла | `Edit` | Находит `old` в файле, заменяет на `new`; ошибка если не уникально |
| `shell.exec(cmd)` | Выполнить shell-команду | `Bash` | Возвращает stdout+stderr+exit_code |
| `agent.delegate(model, prompt)` | Делегировать субагенту | `Agent` tool | Запускает LLM-агент с изолированным контекстом; возвращает ответ |
| `skill.invoke(skill_id, args)` | Вызвать скилл IWE | `Skill` tool | Ищет SKILL.md по `skill_id`, исполняет инструкции |

**Замечание:** хост НЕ обязан реализовывать инструменты с теми же именами. Достаточно предоставить функциональный эквивалент, зарегистрированный в адаптере (§4).

### B. Lifecycle Events — события жизненного цикла

Хост ОБЯЗАН эмитить следующие события; IWE-хуки подписываются на них:

| Событие | Когда возникает | Обязательный payload | CC-эквивалент |
|---------|----------------|---------------------|---------------|
| `SESSION_START` | Начало новой сессии пилота | `session_id`, `agent_model`, `timestamp` | `UserPromptSubmit` |
| `SESSION_END` | Завершение сессии (явное или по таймауту) | `session_id`, `duration_sec`, `exit_reason` | `Stop` |
| `TOOL_EXECUTED` | После выполнения любого инструмента | `session_id`, `tool_name`, `result_summary`, `timestamp` | `PostToolUse` |

**Расширенные события (опциональные для v0.1, обязательные для v1.0):**

| Событие | Когда возникает | Payload |
|---------|----------------|---------|
| `TASK_CREATED` | Вызов task_tracker.create | `task_ids[]`, `session_id` |
| `TASK_COMPLETED` | Статус задачи → completed | `task_id`, `session_id` |
| `SKILL_INVOKED` | Начало выполнения скилла | `skill_id`, `session_id` |

### C. Environment Variables — переменные окружения

Хост ОБЯЗАН предоставить следующие переменные в окружении хуков и скриптов:

| Переменная | Тип | Описание | CC-реализация |
|------------|-----|----------|---------------|
| `IWE_RUNTIME` | string | Имя хоста: `claude-code` \| `hermes` \| `bot` \| `headless` | Устанавливается CC-адаптером как `claude-code` |
| `AGENT_SESSION_ID` | string | Уникальный ID текущей сессии | Берётся из payload CC-хука или генерируется |
| `AGENT_TASK_ID` | string\|empty | ID задачи, если сессия запущена через task-dispatcher | `$CLAUDE_TASK_ID` |
| `AGENT_MODEL_ID` | string | Идентификатор модели LLM | `$CLAUDE_AGENT_ID` или `claude-sonnet-4-6` |
| `IWE_STATE_DIR` | path | Директория для state-файлов хуков | `~/.iwe/state/` (дефолт) |

### D. Configuration — конфигурация

Хост ДОЛЖЕН предоставить механизм для:

1. **Регистрации хуков на события** — хук = shell-скрипт, вызываемый при событии типа B.
   - CC: `settings.json → hooks → [{matcher, hooks: [{type, command}]}]`
   - Hermes: свой формат (TBD в Ж-Ф4)

2. **Регистрации скиллов** — скилл = директория с SKILL.md и точкой входа.
   - CC: `.claude/skills/<id>/SKILL.md` + `/slash-команда`
   - Hermes: своя конфигурация (TBD)

---

## 3. Матрица совместимости хостов

| Компонент | Claude Code | Hermes (план) | Бот (TG) | Headless CLI |
|-----------|-------------|---------------|----------|--------------|
| Tool API | ✅ Нативно | 📋 Проектируется | ⚠️ Частично (нет fs.*) | ⚠️ Частично (нет skill.*) |
| SESSION_START/END | ✅ Via hooks | 📋 В дизайне | ⚠️ Частичная эмуляция | ✅ Stop хук работает без обёртки |
| TOOL_EXECUTED | ✅ PostToolUse | 📋 В дизайне | ❌ Нет | ✅ PostToolUse работает без обёртки |
| IWE_RUNTIME | ✅ CC-адаптер | 📋 Hermes-адаптер | 📋 bot-адаптер | 📋 headless-адаптер |
| AGENT_SESSION_ID | ✅ CC-адаптер | 📋 Hermes native | ⚠️ update_id как proxy | ✅ UUID генерируется |
| Скиллы (invoke) | ✅ `/slash` | 📋 Аналог | ⚠️ Команды TG | ⚠️ Аргументы CLI |

**Легенда:** ✅ реализовано / 📋 запланировано / ⚠️ частично / ❌ отсутствует

---

## 4. Адаптер — структура

Каждый хост поставляет **адаптер** — тонкий слой, который:
1. Устанавливает переменные окружения (§C) при старте сессии
2. Регистрирует хуки на нативные события хоста, транслируя в Lifecycle Events (§B)
3. Регистрирует алиасы инструментов для Tool API (§A)

**Адаптеры:**
- `DP.IWE.011-adapters/claude-code-adapter.md` — референсная реализация (Ж-Ф3, готово к написанию)
- `adapters/hermes.md` — после Встречи 20 с Андреем (Ж-Ф4)
- `adapters/bot.md` — по мере необходимости
- `adapters/headless.md` — по мере необходимости

**Принцип:** адаптер НЕ меняет протоколы IWE. Адаптер — это клей между нативным API хоста и контрактом IWE.

---

## 5. Контракт ОРЗ-протоколов

После внедрения Host Contract протоколы open/work/close ДОЛЖНЫ писаться через абстрактный API:

```
# Было (CC-специфично):
TodoWrite: [{content: "шаг 1", status: "pending"}]

# Должно быть (абстрактно):
task_tracker.create([{content: "шаг 1", status: "pending"}])
```

**Переходный период:** CC-адаптер (Ж-Ф3) транслирует `task_tracker.*` → `TodoWrite`, `scheduler.*` → `ScheduleWakeup`. Протоколы продолжают работать в CC без изменений.

**Правило резолюции (закреплено пир-сессией WP-564, 11.09.2026, Claude+Codex).** Абстрактные операции (`task_tracker.*`, `scheduler.*` и т.п.) резолвятся ИСКЛЮЧИТЕЛЬНО через адаптер, соответствующий текущему `$IWE_RUNTIME`. Прямой вызов host-native инструмента (`TodoWrite`, `ScheduleWakeup` и т.п.) в тексте протокола или скилла в обход адаптера — нарушение контракта; конкретное имя инструмента хоста допустимо только внутри самого файла адаптера (`DP.IWE.011-adapters/*.md`).

**Пошаговые инварианты `task_tracker.*` для многошагового протокола (Day/Week Open, Close).** Один прогон протокола = N единиц отслеживания (по одной на шаг алгоритма), не одна на весь прогон:
1. хост-раннер верно распознан (`$IWE_RUNTIME` соответствует фактическому хосту);
2. перед первым содержательным шагом создан полный список шагов;
3. каждый шаг проходит `pending → in_progress → completed` либо помечается `blocked` (не пропускается молча);
4. одновременно не более одного шага в статусе `in_progress`;
5. следующий шаг не начинается до терминального статуса предыдущего (`completed`/`blocked`);
6. нет вызова host-native инструмента в обход адаптера;
7. прогон достигает предусмотренной протоколом контрольной точки (не обрывается раньше);
8. по завершении прогона число units со статусом `completed`/`blocked` равно числу шагов алгоритма.

Эти 8 инвариантов — единственный обязательный список для проверки соответствия (§9) в любом хосте, включая headless; §9 не дублирует их отдельным перечнем.

---

## 6. Версионирование

| Версия | Обязательные компоненты | Статус |
|--------|------------------------|--------|
| v0.1 | Tool API (A) + переменные (C) + SESSION_START/END (B) | 📋 Draft (этот документ) |
| v0.2 | + TOOL_EXECUTED + TASK_CREATED/COMPLETED | ⏳ После Ж-Ф3 |
| v1.0 | Полный контракт + все расширенные события | ⏳ После Ж-Ф4 (Hermes) |

**Совместимость:** хост с контрактом v0.2 совместим с протоколами, написанными под v0.1.

---

## 7. Связи

- **DP.IWE.001** §5.2 — архитектурная позиция IWE как дистрибутива (слой 4)
- **DP.SC.046** — runtime-цикл open→work→close (что IWE обещает пилоту; этот документ — что хост обещает IWE)
- **DP.SC.019** — автономный cloud runtime (будущий хост на основе Hermes)
- **WP-337 Ж-Ф1** — аудит зависимостей (`направление-Ж-ф1-audit.md`)
- **WP-337 Ж-Ф3** — адаптер Claude Code (`DP.IWE.011-adapters/claude-code-adapter.md`)

---

## 8. Headless-режим (claude -p)

**Ключевой инсайт:** хуки IWE (PostToolUse/Stop из settings.json) срабатывают при `claude -p` без изменений конфигурации. Headless-режим = полноценный runtime-хост по DP.IWE.011.

**Практические следствия:**
- `TOOL_EXECUTED` (PostToolUse) эмитируется в headless-режиме — хуки получают события штатно
- `SESSION_END` (Stop) эмитируется при завершении `claude -p` — close-хуки выполняются
- TaskTracker в headless реализуется через файловую систему (tasks/*.md) вместо встроенного TodoWrite
- Scheduler в headless = внешний cron/launchd с параметром `due:` в task-файле

**См. также:** DP.SOTA.028 (Claude CLI headless hook inheritance), DP.D.083 (Persistent TaskTracker ≠ Ephemeral TodoWrite).

---

## 9. Conformance status — кто реально потребляет контракт

**Найдено пир-сессией (WP-526, 2026-09-07):** контракт специфицирован с 21 мая 2026, но живые ОРЗ-протоколы IWE написаны не против него, а напрямую против инструментов Claude Code — §5 («Переходный период») описывает целевое состояние, которое ещё не наступило.

| Потребитель | Ожидаемое (§5) | Фактическое | Статус |
|---|---|---|---|
| `memory/protocol-open.md` | вызов через `task_tracker.create` | ссылка на `.claude/skills/day-open/SKILL.md`, которая теперь сама вызывает `task_tracker.create` (см. следующую строку) | ✅ текстово мигрировал (CC) |
| `.claude/skills/day-open/SKILL.md` | вызов через `task_tracker.create` | исполняющая инструкция без `TodoWrite`; ссылка на `DP.IWE.011` и адаптеры | ✅ CC-интерфейс: текстово мигрировано; ✅ headless: подтверждён живым прогоном (Фаза 2, см. ниже), кроме гранулярности N-файлов |
| `memory/protocol-work.md` | вызов через абстрактный API | нет упоминания контракта | ❌ не потребляет |
| `memory/protocol-close.md` | вызов через `task_tracker.create` | прямой `TodoWrite` (строка 22: «Day/Week Close = через SKILL.md + TodoWrite») | ❌ не потребляет (вне объёма WP-564 Фаза 1) |
| `CLAUDE.md` / `AGENTS.md` | ссылка на Host Contract при описании агентного ядра | нет упоминания `DP.IWE.011` / `DP.SC.046` | ❌ не потребляет |
| `DP.IWE.011-adapters/claude-code-adapter.md` | маппинг контракт↔CC | есть (§A-D этого адаптера) | ✅ потребляет (декларативно) |
| `DP.IWE.011-adapters/headless-adapter.md` | маппинг контракт↔headless, agent-driven mid-session update | обновлён (single-writer-инвариант, N task-файлов на прогон, commit-контракт по фазам, WP-564 Ф3) | ✅ спецификация; ✅ агент реально создаёт N файлов-шагов (подтверждено живым прогоном); ✅ персистентность починена (`_collect_extra_task_files()` safety-net в диспетчере, WP-564 Ф3, 12.09) |
| `scripts/headless-runner.sh` | headless-адаптер по контракту | ссылается на `DP.IWE.011-adapter-headless` в комментариях, устанавливает `IWE_RUNTIME=headless` | ✅ потребляет адаптер; ✅ живой прогон пройден (пилот, tsekh-1, 12.09) |
| `.claude/settings.json` (интерактивный CC) | `IWE_RUNTIME=claude-code` в `env` (по `claude-code-adapter.md` §C) | установлен (WP-564 Фаза 1) | ✅ потребляет |

**Вывод (обновлено WP-564 Фаза 1, 11.09.2026):** `protocol-open.md`/`day-open/SKILL.md` текстово переписаны против абстрактного API и не упоминают `TodoWrite`; резолюция в Claude Code подтверждена (адаптер уже был `status: active`, маппинг не менялся). `protocol-work.md`/`protocol-close.md` остаются вне объёма.

**WP-564 Фаза 2 (12.09.2026, пир-сессия Claude+Kimi+Codex + реальный прогон пилотом на tsekh-1, live smoke против `TserenTserenov/DS-wp-sandbox`, ветка `smoke/wp564-2026-09-12`) — частичный результат, живым `claude -p` invoke:**

Пилот запустил ту же команду (без `--dry-run`) в своём терминале на tsekh-1 — вне permission-классификатора интерактивной сессии, который блокировал реальный вызов внутри пир-сессии. Прогон завершился успешно (`exit=0`, `claude done ok=True duration=98s`, диспетчер обработал 1/1 задачу).

Подтверждено по факту:
- Инвариант 1 (`$IWE_RUNTIME=headless` резолвится верно) — ✅ подтверждён.
- Инвариант 2 (task-юнит создан до первого шага) — ✅ подтверждён на уровне протокола; **дополнительно подтверждено эмпирически**, что агент внутри `claude -p` СПОСОБЕН сам создать N файлов-юнитов по шагам через нативный `Write` (см. находку ниже про персистентность).
- Инвариант 6 (нет host-native вызова в обход адаптера) — ✅ подтверждён, в тексте промпта и в выводе агента упоминаний `TodoWrite` нет.
- Инвариант 7 (прогон достигает контрольной точки) — ✅ подтверждён: `TASK-2026-09-12-wp564-smoke.md` дошёл до статуса `completed` (не `blocked` на ожидании согласования) — pre-authorized формулировка задания сработала как задумано.
- Инварианты 3, 4, 5 (реальные переходы `pending→in_progress→completed`, не более одного `in_progress` одновременно, корректная последовательность) — **подтверждены лишь косвенно**: 4 файла-шага, реально созданных агентом (`wp564-smoke-step-01..04.md`), содержат ТОЛЬКО финальный статус `completed` — ни один не сохранил промежуточное состояние `pending`/`in_progress` на диске. Единственное косвенное свидетельство последовательности — снимок `wp564-smoke-open-sessions.log`, зафиксировавший шаги 1-3 как `✅`, а шаг 4 как `⏳ (in progress)` в момент своей записи. Это подтверждает порядок, но не строго доказывает соблюдение single-flight инварианта 4 (агент мог в принципе делать это не строго последовательно, просто в данном случае сделал последовательно).
- Инвариант 8 (число `completed`/`blocked` unit'ов = числу шагов алгоритма) — **двойное чтение**: если считать единицей отслеживания только сам `TASK-*.md` (то, что реально закоммичено и опубликовано в git) — 1 unit, 1 шаг, соответствие тривиально выполнено. Если считать единицей отслеживания 4 запрошенных шага алгоритма (как того требует §5 и headless-adapter.md таблица A) — агент создал ровно 4 файла и все 4 отметил `completed`, то есть **на уровне поведения агента инвариант выполнен** (4=4) — но см. находку ниже: эти 4 файла никогда не попадают в официальную (закоммиченную) историю.

**НАХОДКА (эмпирически подтверждена прогоном, не спекуляция по коду) — ПОЧИНЕНО (WP-564 Ф3, 12.09.2026, пир-сессия Claude+Kimi+Codex):** агент действительно создал 4 файла-шага (`wp564-smoke-step-01.md` … `-04.md`) и файл журнала прямо в рабочей копии — это ровно то поведение, которое `headless-adapter.md` таблица A обещает («создаётся... внутри `claude -p` через `Write`»). Но диспетчер (`iwe-agent-dispatcher.py`, комментарий «Dispatcher is the single writer of result files», RC3-fix — номер строки сдвинулся после вставки фикса Ф3, искать по тексту комментария) сразу после возврата `claude -p` делал `git fetch` + `git reset --hard origin/<branch>`, а затем стейджил только два жёстко прописанных пути — `TASK-*.md` и `RESULT-*.md`, из-за чего файлы, созданные агентом, оставались untracked и терялись.

**Фикс:** добавлена функция `_collect_extra_task_files()` (safety-net, вызывается перед финальным `commit_and_push` в LLM-ветке `process_task()`) — подбирает любые untracked/modified `.md` в `inbox/agent/tasks/` (кроме `task_path`/`result_path`, кроме удалений), без фильтрации по `id`/`task_id` (проверено эмпирически: у внешней задачи и у файлов-шагов разные поля frontmatter, фильтр по значению не совпал бы). Опирается на уже существующую строгую последовательность `main()` (for-loop + `acquire_lock()`) — параллельная запись в `inbox/agent/tasks/` архитектурно исключена, остаточный риск (мусор от прерванного прошлого прогона) задокументирован в docstring, не устранён отдельно (не блокер). Реализация верифицирована живым тестом на синтетическом git-репозитории, воспроизводящем ровно найденный сценарий (шаг-файл подобран, task/result-файлы корректно исключены). Полный commit-контракт по фазам («кто коммитит») — в `headless-adapter.md` новая секция после single-writer-инварианта.

**Побочные технические находки Ф2 (не блокеры):** (1) `~/IWE/scripts/headless-runner.sh` разошёлся с `~/IWE/DS-my-strategy/scripts/headless-runner.sh` (первый новее на один фикс, второй — единственный с рабочей парой `iwe-agent-dispatcher.py` рядом; буквальный запуск из корня падает на отсутствии диспетчера) — Drift Reporting, не устранено в рамках Ф2; (2) `DS-my-strategy/scripts/headless-runner.sh:60` (копия, реально задействованная в Ф2 — вторая копия в корне `~/IWE/scripts/` разошлась, см. п.1) падает на `md5: command not found` на Linux-хостах (нужен `md5sum`), fallback срабатывает, но шумит в stderr; (3) GOV_BRANCH по умолчанию `main` без отдельного `--target-branch` — реальный live-прогон без явного `IWE_DISPATCHER_REPO_BRANCH` рискует запушить в главную ветку целевого репозитория (в этом прогоне переменная была задана явно, риск не реализовался); (4) стейджинг дисциплина диспетчера (`git add <2 жёстких пути>` вместо `git add inbox/agent/**`) — прямая причина находки о потере N-файлов выше, не отдельный баг, а тот же корень.

**Следующий шаг:** код починен (см. выше), но фикс не проверен ЖИВЫМ headless-прогоном (только синтетическим тестом на изолированном git-репозитории) — реальная проверка требует повторного `claude -p` invoke, снова недоступного из интерактивной сессии (тот же permission-классификатор). Статус `draft` в frontmatter `DP.IWE.011` поднимать до `active` после живого подтверждения фикса на tsekh-1 (та же схема, что и для Ф2 — пилот в своём терминале).
